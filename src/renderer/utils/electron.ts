const electron = require('electron')
const {
  clipboard,
  contextBridge,
  crashReporter,
  desktopCapturer,
  ipcRenderer,
  nativeImage,
  webFrame
} = electron

export default electron
export {
  clipboard,
  contextBridge,
  crashReporter,
  desktopCapturer,
  ipcRenderer,
  nativeImage,
  webFrame
}
